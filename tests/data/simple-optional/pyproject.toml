[project]
name = "test-pdm"
version = "0.1.0"
dependencies = ["requests==2.31.0"]
requires-python = ">=3.8"

[project.optional-dependencies]
socks = ["PySocks==1.7.1"]

[tool.pdm.dev-dependencies]
test = ["pytest==7.4.0"]